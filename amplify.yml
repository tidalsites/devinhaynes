version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 24.14.0
        - nvm use 24.14.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
